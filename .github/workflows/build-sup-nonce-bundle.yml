name: Build SUP nonce scan bundle

on:
  workflow_dispatch:
  push:
    tags:
      - "scan-sup-nonces-*"

permissions:
  contents: write

jobs:
  bundle:
    name: Bundle for JavaScriptCore / a-Shell
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Bundle nonce scanner
        run: npm run bundle:nonces

      - name: Upload bundle artifact
        uses: actions/upload-artifact@v6
        with:
          name: scan-sup-nonces-javascriptcore
          path: scan-sup-nonces.bundle.js
          if-no-files-found: error

      - name: Attach bundle to tag release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: scan-sup-nonces.bundle.js
